---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/layout/Nav.astro';
import Footer from '../components/layout/Footer.astro';
import Hero from '../components/sections/Hero.astro';
import Services from '../components/sections/Services.astro';
import Pricing from '../components/sections/Pricing.astro';
import Process from '../components/sections/Process.astro';
import Trust from '../components/sections/Trust.astro';
import Contact from '../components/sections/Contact.astro';
import FAQ from '../components/sections/FAQ.astro';

// Content imports
import company from '../content/en/company.json';
import services from '../content/en/services.json';
import pricing from '../content/en/pricing.json';
import process from '../content/en/process.json';
import trust from '../content/en/trust.json';
import contact from '../content/en/contact.json';
import faq from '../content/en/faq.json';

const lang = 'en' as const;
const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faq.items.map((item) => ({
    "@type": "Question",
    "name": item.question,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": item.answer
    }
  }))
};
---

<BaseLayout
  title="Affordable Small Business Website Creation | Pixflow Studio"
  description="Affordable website creation for small businesses, startups, and local services. Fast website setup, hosting included, and launch in 48-72 hours from €25/month."
  lang={lang}
  schemaOverrides={[faqSchema]}
>
  <Nav lang={lang} nav={company.nav} />

  <main>
    <Hero hero={company.hero} lang={lang} />
    <Services services={services} />
    <Pricing pricing={pricing} />
    <Process process={process} />
    <Trust trust={trust} />
    <FAQ faq={faq} />
    <Contact contact={contact} lang={lang} />
  </main>

  <Footer
    lang={lang}
    footer={company.footer}
    email={company.email}
    whatsapp={company.whatsapp}
  />
</BaseLayout>
